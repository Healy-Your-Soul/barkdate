# Google Places API Configuration
# 
# To use the Google Places API functionality in the map screen, you need to:
# 1. Enable the Google Places API in the Google Cloud Console
# 2. Create an API key with Places API permissions
# 3. Set the API key as an environment variable when running the app
#
# Example:
# flutter run -d chrome --dart-define=GOOGLE_PLACES_API_KEY=your_api_key_here
#
# For production builds:
# flutter build web --dart-define=GOOGLE_PLACES_API_KEY=your_api_key_here

GOOGLE_PLACES_API_KEY=your_api_key_here
